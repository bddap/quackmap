[package]
name = "quackmap"
version = "0.1.0"
edition = "2024"
description = "A fixed-limit on-disk hashmap with variable-length keys and values."

[dev-dependencies]
criterion = "0.5"
memmap2 = "0.9.5"
rand = "0.9.0"
tempfile = "3.19.0"

[[bench]]
name = "benches"
harness = false
